version: '3.8'

services:
  terrainvision-ai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: terrainvision_app
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - FLASK_ENV=production
    restart: unless-stopped
